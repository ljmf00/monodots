apiVersion: v1
kind: Secret
metadata:
  name: monodots-searxng-secret
  labels:
    app.kubernetes.io/name: searxng
    app.kubernetes.io/component: secret
  annotations:
    helm.sh/resource-policy: keep
type: Opaque
immutable: true
stringData:
  # REPLACE this value at deploy time via Infisical or manual injection —
  # do NOT commit a real secret value to version control.
  SEARXNG_SECRET: PLACEHOLDER_CHANGE_ME
